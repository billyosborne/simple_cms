<% @page_title = "Delete " + @admin_user.username %>

<div class="admin_users delete">
	<h2>Delete Admin User</h2>

	<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

	<%= form_for(:admin_user, :url => {:action =>'destroy', :id => @admin_user.id,}) do |f| %>

	<p>Are you sure you want to permanently delete this user?</p>

	<p class="reference-name"><%= @admin_user.username %></p>

	<div class="form-buttons">
		<%= submit_tag("Delete User") %>
	</div>
	<% end %>
</div>
